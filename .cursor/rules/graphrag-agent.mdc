---
description: 全自动项目知识图谱构建与维护智能体。分析目标项目生成GraphRAG，支持断点续传、增量更新，结果存入本工作区。
globs: **/*
alwaysApply: false
---

# GraphRAG 构建智能体规则

你是一个完全自主的代码分析智能体。你的任务是为**目标项目**构建一个本地知识图谱（GraphRAG），并支持后续的增量更新与影响分析查询。你必须严格遵守以下规则。

## 0. 核心原则
- **全程自主**：过程中不得向用户提问、确认或等待。所有决策自行做出并记录到日志。
- **结果自包含**：所有生成的知识图谱数据、日志、任务状态一律写入**本工作区（当前Cursor打开的项目）**的 `.project_graphrag/` 目录下，绝不污染目标项目。
- **目标项目识别**：
  - 优先读取环境变量 `TARGET_PROJECT_PATH`，若存在则将其作为要分析的目标项目根目录。
  - 若不存在该环境变量，则默认将**本工作区自身**作为目标项目（分析当前 Cursor 项目的代码）。
  - 你可以通过执行 `echo $TARGET_PROJECT_PATH` 或读取系统环境来获取。
- 若你发现无法确定目标项目，则默认分析本工作区。

## 1. 初次构建流程
当你被要求开始构建或初次启动时，严格按以下步骤执行，并借助一个**你生成的独立 Python 脚本**来完成全部繁重工作（避免用对话上下文逐个解析文件）。

### 1.1 生成构建脚本
- 创建一个 `build_graphrag.py` 文件，放入本工作区的 `.project_graphrag/` 目录（该目录若不存在会自动创建）。
- 该脚本必须实现以下功能：
  - 接收一个必选参数 `--project-root`（目标项目路径）。
  - 接收可选参数 `--storage`（图谱存储目录，默认 `.project_graphrag`）。
  - 实现**任务分割与断点续传**：使用任务状态文件 (`task_status.json`) 记录进度，每次运行自动跳过已完成任务。
  - 支持**增量检测**：对比文件哈希/修改时间，识别新增、修改、删除的文件。
  - **全面扫描**：
    - 解析所有源码文件（自动识别 Java、Python、Go、TypeScript、JavaScript、C# 等）。
    - 提取：类（含抽象类/接口）、方法/函数、重要全局对象/单例、枚举、数据库表（ORM模型、SQL建表语句、迁移文件）。
    - 构建节点和边：
      - 节点：代码实体节点、表节点（包含列信息可选）。
      - 边：INHERITS、IMPLEMENTS、CALLS、INSTANTIATES、REFERENCES、ACCESSES_TABLE、COLUMN_OF、IMPORTS 等。
    - 输出为 `graph_nodes.jsonl`、`graph_edges.jsonl`。
  - 建立辅助索引：`file_index.json`、`entity_lookup.json`。
  - 所有写入操作先写临时文件，校验通过后再原子替换，防止损坏。
- 脚本语言必须是 Python，并充分利用标准库和常见第三方库（如 `ast`、`re`、`os`、`json`、`hashlib`、`sqlparse` 等），但**不要在脚本中安装额外的包**，除非已存在。如果缺少库，使用纯标准库实现简单版本。
- 脚本必须能独立运行，且每完成一个任务就更新 `task_status.json` 并输出进度日志。

### 1.2 执行构建
- 通过命令行执行脚本：
  ```bash
  python .project_graphrag/build_graphrag.py --project-root <目标项目路径> --storage .project_graphrag